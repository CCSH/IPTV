name: 'WhiteList BlackList'
on:
  schedule:
    - cron: '0 16 * * 4'
  workflow_dispatch:

jobs:
  run-whitelist-blacklist:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    defaults:
      run:
        working-directory: ${{ github.workspace }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 安装系统依赖
        run: sudo apt-get update -y && sudo apt-get install -y --no-install-recommends ffmpeg && ffmpeg -version && ffprobe -version

      - name: 安装Python依赖
        run: python -m pip install --upgrade pip --no-cache-dir && pip install opencc-python-reimplemented --no-cache-dir
        env:
          PIP_CACHE_DIR: ${{ runner.temp }}/pip-cache

      - name: 运行脚本
        run: |
          chmod +x assets/whitelist-blacklist/main.py
          python assets/whitelist-blacklist/main.py

      - name: 配置Git
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local core.autocrlf false
          git config --local core.filemode false

      - name: 暂存文件
        run: git add --force assets/whitelist-blacklist/*.txt
        continue-on-error: true
        id: stage-files
  
      - name: 提交并【强制覆盖】推送
        run: |
          if git diff --cached --name-only | grep -q .; then
            git commit -m "黑白名单 Update: $(TZ='Asia/Shanghai' date +'%Y-%m-%d') (CST)"
            git push origin HEAD:$GITHUB_REF_NAME --force
            echo "✅ 强制推送完成"
          else
            echo "ℹ️ 无变更，跳过推送"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        if: steps.stage-files.outcome == 'success'
